# Database
DATABASE_URL=postgresql+asyncpg://postgres:changeme@localhost:5432/datatchek
DATABASE_URL_SYNC=postgresql+psycopg2://postgres:changeme@localhost:5432/datatchek

# Redis
REDIS_URL=redis://localhost:6379

# Security - GENERATE NEW KEYS FOR PRODUCTION
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=CHANGE_ME_IN_PRODUCTION_MIN_32_CHARS

# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=CHANGE_ME_FERNET_KEY_44_CHARS_BASE64

# JWT
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS
CORS_ORIGINS=["http://localhost:3000","http://localhost:5173"]

# Debug
DEBUG=false
